Changes in component 'nf-core/staramr/search'
Changes in 'staramr/search/meta.yml':
--- modules/nf-core/staramr/search/meta.yml
+++ modules/nf-core/staramr/search/meta.yml
@@ -30,6 +30,11 @@
           Assembled/complete genome(s) in FASTA format to search for AMR/MLST/Plasmids.
         pattern: "*.{fasta,fna,fsa,fa,fasta.gz,fna.gz,fsa.gz,fa.gz}"
         ontologies: []
+    - pointfinder:
+        type: string
+        description: |
+          Organism to use with PointFinder database.
+        pattern: "campylobacter|enterococcus_faecalis|enterococcus_faecium|escherichia_coli|helicobacter_pylori|mycobacterium_tuberculosis|neisseria_gonorrhoeae|plasmodium_falciparum|salmonella|staphylococcus_aureus"
 output:
   results_xlsx:
     - - meta:

Changes in 'staramr/search/main.nf':
--- modules/nf-core/staramr/search/main.nf
+++ modules/nf-core/staramr/search/main.nf
@@ -4,11 +4,11 @@
 
     conda "${moduleDir}/environment.yml"
     container "${ workflow.containerEngine == 'singularity' && !task.ext.singularity_pull_docker_container ?
-        'https://depot.galaxyproject.org/singularity/staramr:0.10.0--pyhdfd78af_0':
-        'biocontainers/staramr:0.10.0--pyhdfd78af_0' }"
+        'https://depot.galaxyproject.org/singularity/staramr:0.11.0--pyhdfd78af_0':
+        'biocontainers/staramr:0.11.0--pyhdfd78af_0' }"
 
     input:
-    tuple val(meta), path(genome_fasta) // genome as a fasta file
+    tuple val(meta), path(genome_fasta), val(pointfinder) // genome as a fasta file
 
     output:
     tuple val(meta), path("*_results/results.xlsx")        , emit: results_xlsx
@@ -29,6 +29,7 @@
     def prefix = task.ext.prefix ?: "${meta.id}"
     def is_gzipped = genome_fasta.getName().endsWith(".gz") ? true : false
     def genome_uncompressed_name = genome_fasta.getName().replace(".gz", "")
+    def org = pointfinder ? "--pointfinder-organism ${pointfinder}" : ""
     """
     if [ "$is_gzipped" = "true" ]; then
         gzip -c -d $genome_fasta > $genome_uncompressed_name
@@ -37,6 +38,7 @@
     staramr \\
         search \\
         $args \\
+        $org \\
         --nprocs $task.cpus \\
         -o ${prefix}_results \\
         $genome_uncompressed_name

Changes in 'staramr/search/environment.yml':
--- modules/nf-core/staramr/search/environment.yml
+++ modules/nf-core/staramr/search/environment.yml
@@ -6,5 +6,5 @@
 
 dependencies:
   - bioconda::mlst=2.23.0
-  - bioconda::staramr=0.10.0
+  - bioconda::staramr=0.11.0
   - conda-forge::numpy=1.25.2

'modules/nf-core/staramr/search/tests/main.nf.test' is unchanged
'modules/nf-core/staramr/search/tests/main.nf.test.snap' is unchanged
************************************************************
