Changes in component 'nf-core/sylph/sketch'
'modules/nf-core/sylph/sketch/meta.yml' is unchanged
Changes in 'sylph/sketch/main.nf':
--- modules/nf-core/sylph/sketch/main.nf
+++ modules/nf-core/sylph/sketch/main.nf
@@ -1,18 +1,18 @@
 process SYLPH_SKETCH {
     tag "$meta.id"
-    label 'process_high'
+    label 'process_quick'
+    label 'error_ignore'
 
     conda "${moduleDir}/environment.yml"
     container "${ workflow.containerEngine == 'singularity' && !task.ext.singularity_pull_docker_container ?
-        'https://depot.galaxyproject.org/singularity/sylph:0.7.0--h919a2d8_0' :
-        'biocontainers/sylph:0.7.0--h919a2d8_0' }"
+        'https://depot.galaxyproject.org/singularity/sylph:0.8.1--ha6fb395_0' :
+        'biocontainers/sylph:0.8.1--ha6fb395_0' }"
 
     input:
     tuple val(meta), path(reads)
-    path(reference)
 
     output:
-    tuple val(meta), path('my_sketches/*.sylsp'), path('database.syldb'), emit: sketch_fastq_genome
+    tuple val(meta), path('my_sketches/*.sylsp'), emit: sketch_fastq_genome, optional: true
     path "versions.yml"                                                 , emit: versions
 
     when:
@@ -26,7 +26,6 @@
     sylph sketch \\
         $args \\
         $fastq \\
-        -g $reference \\
         -S $prefix \\
         -d my_sketches
 
@@ -41,7 +40,6 @@
     """
     mkdir -p my_sketches
     touch my_sketches/${prefix}.sylsp
-    touch database.syldb
 
     cat <<-END_VERSIONS > versions.yml
     "${task.process}":

Changes in 'sylph/sketch/environment.yml':
--- modules/nf-core/sylph/sketch/environment.yml
+++ modules/nf-core/sylph/sketch/environment.yml
@@ -4,4 +4,4 @@
   - conda-forge
   - bioconda
 dependencies:
-  - bioconda::sylph=0.7.0
+  - bioconda::sylph=0.8.1

'modules/nf-core/sylph/sketch/tests/main.nf.test' is unchanged
'modules/nf-core/sylph/sketch/tests/main.nf.test.snap' is unchanged
************************************************************
